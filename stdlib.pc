_MAIN():
	Push(0, x)
	Pop(a, stdin)
	Push(a, x)
	Push(0, y)
	Pop(a, stdin)
	Push(a, y)
	Push(0, z)
	Pop(a, stdin)
	Push(a, z)
	If (
		(x>y),
		If (
			(z>x),
			Print(z),
			Print(x)
		),
		Print(y)
	)
;

_Print(x):
	Push(x, stdout)
;

##Div(h, i, j):
##