@stdlib

_Main():
	Push(0, z)
	Stdlib.Print("Which prime do you want?:")
	Pop(z, stdin)
	Push([], x)
	Push(4, y)
	Push(y, bignum)
	Push(0, a)
	_Outer():
		Stdlib.For(x, Main.Inner())
		Push(1, y)
		Push(y, bignum)
		If(
			(z>x),
			Main.Outer()
		)
	;
	_Inner():
		If(
			(prime>bignum),
			Push(y, x),
			Main.CheckPrime()
		)
	;	
;
